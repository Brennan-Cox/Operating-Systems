This project was to create a shell that could execute programs

NOTE: shell1 cannot run on windows operating systems only linux

shell1:
A shell that uses forking to execve a given program

shell2:
A shell that uses threading to execute a given program
